# Feature Log

Running list of features/fixes worked on, for release tracking. Newest first within each section. Date = last commit date on the branch.

## Next Release

| Date | Feature | Branch | Repos affected | Sandbox |
|---|---|---|---|---|
| 2026-09-01 | Admin portal auth + audit: wire Cognito login (incl. temp-password challenge) into web-ui-admin, verify JWTs on every admin-backend endpoint (fail-closed), per-user session ownership, structured `admin-audit` log events (chat, tool use w/ demographic redaction), CORS allowlist; remove dead browser→Lambda invocation path; new `cognito-admin` TF module for the dedicated admin user pool; agent moved to Bedrock-only via AI SDK (9-model catalog incl. AU Claude 5s / DeepSeek / GLM / GPT-5.6, side-by-side compare, AU data-residency guards, AI SDK UI stream), Anthropic API key retired; org-scope picker; new `read-users-stf` (user-service, branch `feature/admin-org-users-tool`) backing a get_organization_users agent tool; query_partner_api_activity tool (Athena external-api-audit archive); dockerized nginx deploy with per-env ENV_FILE build-arg; env-labelled branding (tab title "Parchment Admin — Dev/Sandbox/Production" + Parchment favicon) | `feature/admin-auth-audit` `feature/admin-org-users-tool`| web-ui-admin, admin-backend, tf-misc-infrastructure, user-service | yes
| 2026-09-19 | Admin portal chat: compare view now opens GLM 5 (left) / Kimi K2.5 (right), replacing DeepSeek V3.2 on the right; both are AU-resident so the compare pair keeps the patient PHI tools. Model catalog left AU-only — Kimi K3 was evaluated and rejected because its sole ap-southeast-2 profile is `global.moonshotai.kimi-k3`, which the residency guard treats as non-AU and which would have stripped `get_patient`/`search_patient_ihi` from the default chat. Known defect not fixed here: the `claude-haiku-4-5` catalog entry fails with ResourceNotFoundException ("Model use case details have not been submitted for this account") | `feature/admin-auth-audit` | web-ui-admin | no |
| 2026-08-31 | MIMS CDS drug interactions (incl. reserved right-rail layout, idle placeholder before medication pick, premium upsell placeholder behind entitlement stub, beta badge, mobile placement above action buttons, manual-add hidden; static copies of MIMS reference vocabulary — doc levels, severities, poison classes, pregnancy/sport categories, DI disclaimers — with definition tooltips on doc level + severity, disclaimer fallback when the live dereference fails, glass tooltip style, severity-tab overflow fix) + "Taking now" clinical flag on prescriptions (single sparse `taking_now` attribute doubling as the TakingNowIndex GSI hash key; PATCH + GET .../taking-now; confirm-dialog toggle on prescription list + details; "only medications marked as taking now" filter on the interaction check with no date window; 183-day stale prompt; fix: interaction check ran against a stale cached medication list, so a script created earlier in the same session was missing from the next check) | `feature/mims-drug-interactions` | partner-service, web-ui, erx-service, common, tf-misc-infrastructure/dynamodb/prescription-db | Yes |
| 2026-07-25 | WAF — Terraform: add api + cognito WAF in prod after deleting manual WAF | `main` | tf-misc-infrastructure |
| 2026-07-25 | Endpoint hardening — email, auth, user, kms-cognito, web-ui | `bug/misc-bugs` | email-service, auth-service, user-service, web-ui |
| 2026-07-24 | Register partner org ops script | `feature/register-partner-org-ops-script` | partner-service |
| 2026-07-13 | Supply-duration estimate (supply-until hint from qty/repeats/dose/frequency) | `feature/supply-duration-algo` | erx-service, web-ui |
| 2026-07-29 | WAF — pin NoUserAgent_HEADER to Count (blocked partner server-to-server token calls) | `feature/misc-tweaks` | tf-misc-infrastructure | yes |

## Backlog

| Date | Feature | Branch | Repos affected |
|---|---|---|---|
| 2026-09-21 | External partner user API data-integrity fixes (partner-reported): Update User no longer reports a failed provider create as success — the create branch discarded `createOrGetProvider`'s return and fell through to 200, so a 409 HPI-I conflict came back as "User updated successfully" with no provider record written (confirmed live on dev). Still to come on the branch: never write a partial provider record (merge stored demographics/`full_name` or 400), treat an HI lookup with no `hpii_status` as a failure, Read User merges `details` instead of replacing it and emits null rather than dropping keys, a booleans-only `prescribing` readiness block so partners can tell whether a user can prescribe, `full_name` write-back, and failed HI lookups mapped to 4xx not 500 | `feature/external-user-data-integrity` | user-service |
| 2026-09-20 | GitHub Actions deploy IAM hardening (tf-misc `vpc` module, already applied in prod but unused): serverless deploy policy scoped to stage-named stacks/buckets/functions/roles/log groups/rules (was `*-service-*` — matched the PHI xml-dump bucket and the Amplify admin role), `kms:Decrypt` pinned to the SSM key via `kms:ViaService`, Route53 writes pinned to the api zone, `iam:PassRole` gated on `iam:PassedToService`, ENI/X-Ray runtime perms dropped; new `pm-<ws>-lambda-permissions-boundary` + `enforce_permissions_boundary` flag (on in production, off in dev/sandbox until every serverless.yml sets `provider.iam.role.permissionsBoundary`) closes the PutRolePolicy→UpdateFunctionCode escalation; policy split in two to fit the 6,144-char limit; new GitHub OIDC provider + `pm-<ws>-github-deploy` role (trust = `repo:ParchmentHealth/<repo>:environment:<ws>`, prod allow-list = admin-backend, web-ui-admin) so the static access key can be retired; identity moved to `vpc/github-deploy.tf` with the same resource addresses (no state moves) | `feature/github-deploy-iam-hardening` | tf-misc-infrastructure |
| 2026-09-20 | Agent Factory: tenant-boundary review 2026-09-19 remediation — per-run secret (`run_token`, sha256 on the record, handed to the worker / admin request scope, never logged; broker refuses id-only calls and legacy records), worker `InvokeGateway` pinned to the one gateway; per-event ownership covers top-level/malformed tags; every output string scrubbed; VPC endpoint policies limit S3 to this account's buckets and the gateway endpoint to this account's gateways; worker image reproducible (digest-pinned base, exact SDK, immutable lockfile install, zip digest); Cedar README corrected to ENFORCE. 104 tests | `dev` (agent-factory, admin-backend), `main` (tf-misc-infrastructure) | agent-factory, admin-backend, tf-misc-infrastructure |
| 2026-09-19 | Agent Factory: security re-review 2026-09-19 remediation (R1–R4 + notification) — common identity is API Gateway's non-overridable `extendedRequestId` (REST `requestId` demoted to a hint, `apigw_request_id`), base64-safe id grammar; broker fences legacy `gateway`/`header`-labelled inbound records; launcher capacity via atomically reserved `SLOT#` leases, abort signals on all I/O within the invocation budget, bounded cleanup; Intercom single-use action token via canvas `stored_data`; launcher re-resolves conversation scope before posting notes. Both re-review reproductions pass as real tests (94 total) | `main` (common), `dev` (agent-factory) | common, agent-factory |
| 2026-09-19 | Agent Factory: security review 2026-09-19 remediation, SR-01/02/03 — common mints the request identity (client `x-correlation-id` becomes a logged hint, partners get the minted id as `requestId`); broker resolves trace ownership with a separate authority query for the inbound audit record across ingress `*-external` log groups (exactly one platform-minted record, complete result), withholds per-event foreign-org lines in traces and recent errors (`withheld_events`), reports row-limit truncation; SR-04 report keys derived by the launcher with a per-run presigned PUT (worker holds no S3 permission); SR-05 typed output projection + scrubbed Intercom summary; SR-06 run records fail closed (full schema, consistent reads, deny-all without gateway config); SR-07 lease-based launcher capacity, shared invocation deadline, zero async retries + failure queue, reserved concurrency and replay dedupe on the Intercom URL; common `invokeLambdaFunction` fix for Event invokes (empty payload was reported as failure). All eight review reproductions now pass as real tests | `main` (common), `dev` (agent-factory) | common, agent-factory |
| 2026-09-19 | Billing past_due lock-out fix: `read-stripe-customer-stf` preferred active/trialing then the NEWEST subscription, so once a real sub went past_due the stray canceled sub left by the reactivation flow won and cached `status: 'canceled'` — `shouldBlockAccess` then locked the org out of a dashboard past_due should still reach, and the blocker's reactivation path short-circuited with `client_secret: null` so the open invoice could never be paid in-app; selector now prefers active > trialing > past_due > newest (`unpaid` deliberately excluded so real revocations still block) + regression tests; web-ui reactivation form no longer claims "Payment received" on a poll timeout when no charge was attempted | `feature/misc-tweaks` | stripe-service, web-ui |
| 2026-09-16 | "Send via SparrowHub" in the prescribe flow, behind the org-level `sparrowhub` feature flag: new accordion section on the V2 submit footer (SparrowHub logo, mobile/email prefilled from the patient — either enables it, each filled field becomes a notification channel — pharmacy location dropdown); lodges the script as Prescribe Only so the token goes to the pharmacy, never the patient, and the patient receives the pharmacy's cart link by SMS/email; erx-service registers a dispensing intent with partner-service at create (raw entitlement facts — partner-service owns the signed-off mapping table: private → private, concession CRN → concession, DVA → concession, Medicare → pbs; Safety Net and CTG ignored) and hands `lodged`/`lodgement_failed` over asynchronously at the exchange ack so the ACTIVE write never waits; the intent becomes the cart on the same dispensing-db row, nothing SparrowHub-shaped on the prescription record; review page shows a "Sending to pharmacy via SparrowHub" step that polls the outcome ≤30 s; prescription list and details show a SparrowHub badge (pending / sent / failed with reason / dispensed) from a batch lookup and hide REISSUE while a pharmacy holds the token; REISSUE slider gains "Send to pharmacy via SparrowHub" for lodged scripts without a live cart (retry after failure, or send a Prescribe Only script on later); cart endpoint accepts raw entitlement facts; dev/sandbox substitute SparrowHub's demo token at the SparrowHub boundary only; eRx conformance gate run — PRES-46A decision (cart link is not an EoP) recorded, three must-review findings logged in the plan | `feature/sparrowhub-dispensing` (partner-service), `feature/sparrowhub-prescribe-flow` (erx-service, web-ui), `main` (common) | partner-service, erx-service, web-ui, common |
| 2026-09-21 | ADHA eRx conformance knowledge base: DH-3728 v3.0.3 rules extracted to `erx-service/docs/conformance` (Open PDS scope, Parchment overlay, generated rule files, coverage script); `erx-conformance` skill gates eRx changes; eRx Payload Requirements for AIP v2.0 (July 2024) added as vendor rules `AIP-1`–`AIP-13` (`source/erx-aip.yaml`, rendered `rules/aip.md`, render/coverage scripts load vendor rule sets, overlay mapped against `getActiveIngredientPrescribingFields`: 7 partial/gap incl. decision `AIP-13` for the blank extemporaneous `PrescribedItem_Reserved_03`); skill triggers on AMT/brand/LEMI payload changes | `main` | erx-service |
| 2026-09-16 | Stripe Subscriptions dashboard: 'Billing Status vs Prescriptions Written' lists every consolidated-partner child org (0 scripts, no Stripe customer) as Partner-billed via a core-db Athena target outer-joined to the CloudWatch rows (new `consolidated_partners` dashboard variable); also seeds rows from 'New Organization' / 'New Partner Integration' events; partner id lowercased. No new IAM (stripe-service role left untouched) | `feature/misc-tweaks` | tf-misc-infrastructure |
| 2026-09-14 | Patient-data retention: 25-year catch-all lifecycle rule on the audit-logs bucket for the eRx prescription audit trail (org-id prefixes, previously kept forever) + `partner-api/` + `partner-gateway/`, all rules now also expire noncurrent versions (bucket is versioned, so `expiration` alone only wrote a delete marker); 6-month (183-day) lifecycle rule on the erx-service and healthcare-services `xml-dump` buckets (raw eRx / HI Service XML with full patient identity, 680k objects with no expiry since Aug 2024) | `feature/audit-logs-prescription-retention` | tf-misc-infrastructure |
| 2026-09-14 | Stripe Subscriptions dashboard: 'New Organizations in Time Range' table (orgs whose sign-up event is inside the time picker — created, org id/name, partner, billing status, prescriptions, owner) + hide the stray CloudWatch 'Value' link column on the org/partner tables | main | tf-misc-infrastructure |
| 2026-09-14 | Stripe snapshot: new `partner_billed` lifecycle bucket — orgs billed through a consolidated partner (org row has integrated_pms and no stripe_customer_id, partner billing_mode CONSOLIDATED_*) no longer count as churned / never-subscribed; aggregate + per-partner `partner_billed_customers`, per-org `billing_mode` + `partner_billing_disabled`; dashboard gets a Partner-billed tile, pie/trend series, partner-table column and org-table colour mappings | `feature/stripe-snapshot-partner-billed` (stripe-service), main (tf-misc-infrastructure) | stripe-service, tf-misc-infrastructure |
| 2026-09-14 | Stripe Subscriptions dashboard: new full-width 'Billing Status vs Prescriptions Written' table — organization id + name, prescriptions written in the time range (erx `Prescription Metrics` Active events) joined to the latest daily Stripe lifecycle bucket (paying/trial/past-due/churned/never-subscribed) with partner + billing email | `feature/stripe-billing-vs-scripts` | tf-misc-infrastructure |
| 2026-09-12 | SparrowHub dispensing status polling → `prescription.dispensed` webhook: sparse `PollDueIndex` on the dispensing table (attributes exist only while a record is pollable, so the index holds exactly the outstanding work) + a 5-minute EventBridge tick that claims each due record by advancing its due time under a condition before any outbound call; tiered decay per kind (cart 30m/3h/12h giving up at 7 d, order 20m/1h/6h at 3 d) with a 06:00-22:00 AEST window, all config; failures back off by cause and never count toward the age cap so an outage cannot expire open scripts; the table now streams NEW_AND_OLD_IMAGES and partner-service consumes its own stream (filtered to `DISPENSING#` modifications) so a transition is reacted to exactly once whichever path wrote it, then delegates to a new erx-service `emit-dispensed-stf` which builds the long-dormant `prescription.dispensed` event and queues it on the existing partner FIFO path, guarding the zero-row prescription lookup that silently loses `prescription.ceased`; stream DLQ; one log line per poll plus a per-tick run line feeding 15 new Grafana panels (volume by org and partner, outcomes, stop reasons, upstream statuses, open records by age, polls per dispense, backlog per tick) | `feature/sparrowhub-dispensing` (partner-service), `feature/sparrowhub-dispensed-webhook` (erx-service), `feature/dispensing-db` (tf-misc-infrastructure) | partner-service, erx-service, tf-misc-infrastructure |
| 2026-09-12 | SparrowHub dispensing integration, phase 1 (manual endpoints only, not wired into the prescription flow): provider-neutral dispensing layer (`DISPENSING_PROVIDER`, new CMK-encrypted `dispensing-db` table with ProviderRefIndex), per-org SparrowHub credentials (KMS-encrypted, validated live on save, SSM stage-default fallback in dev/sandbox only), SparrowHub client (email/password JWT cached 6h in lookup-db, single re-login on 401, no POST retries, Cloudflare-safe user-agent, unwraps string-encoded `data`), SCID → Medication Knowledge resolution with 410 already-dispensed rejection and caller-item fallback, direct order (pickup/courier, fixed courier rule) + cart (patient completes delivery/payment) creation sharing one item mapper, one-live-record-per-token idempotency guard, courier orders quote delivery first (SparrowHub validates delivery total against the method price), status refresh via polling (no SparrowHub webhooks), delivery quotes; per-attempt dispensing audit trail (durable `AUDIT#` item per attempt incl. failures — request/response summaries, step timings, fixed error classes, upstream status, 2-year TTL — plus token-free `dispensing-audit` log line) with `GET .../audit/{token}`; Grafana "Dispensing - SparrowHub" dashboard (Logs Insights) in tf-misc-infrastructure/grafana; 10 Cognito+ACL routes under `/organizations/{id}/dispensing/sparrowhub`; partner CMK key policy now also grants the default partner-service Lambda role; unit + hand-run staging integration tests, smoke + SSM seed scripts; patient cart link corrected to the SparrowCart product after SparrowHub confirmed SparrowScripts is a separate pharmacy-picker product (`feature: carts`, link `https://cart[-stag].sparrowhub.com.au/c/{reference}` keyed by the UUID reference); patient link delivery added — SparrowHub creates no notification for API-made carts, so create-cart now takes `notify` (default `['sms']`, also email or both) and sends the link through SparrowHub's own sender so it arrives from the pharmacy, with a per-stage message template and kill switch, redacted contacts, and every attempt recorded on the response, the dispensing record and the audit trail; sending can never fail cart creation | `feature/sparrowhub-dispensing` (partner-service), `feature/dispensing-db` (tf-misc-infrastructure) | partner-service, tf-misc-infrastructure |
| 2026-09-12 | Address/code-list fields matched case-insensitively — a patient typing `Nsw` into a Cliniko intake form blocked the eScript with an invalid-state error: shared `normalizeAuState` in common (any casing plus the full state name) wired into the Cliniko / CorePlus / Nookal mappers in both partner-service and web-ui; external partner API create + update schemas coerce every code list (`state`, `sex`, `ihi_status`, `ihi_record_status`, `partner_id`, `indigenous_type`, `dva_card_color`) against its own members, email lower-cased, DVA / entitlement numbers upper-cased with separators stripped; CorePlus no longer defaults an unreadable state to `VIC` and keeps multi-word suburbs | `feature/pms-address-case-insensitive`, `main` (common) | partner-service, patient-service, web-ui, common |
| 2026-09-10 | React Doctor: de-complex V3 `IndicationSearch` (lookup hook + results-list/mode-toggle subcomponents, no behaviour change) | `feature/prescription-v3` | web-ui |
| 2026-09-06 | Agent Factory data minimisation (security review 2026-09-03 AF-03, AF-18, AF-26): broker scrubs free-text log fields for IHI/HPI, Medicare, email, AU phone, DOB and name-ish keys before truncation (patterns reported on the audit line), broker role denies `logs:Unmask`; account-level CloudWatch data-protection policy (managed AU identifiers + custom IHI/phone, audit findings log group) in Terraform; Intercom notes now carry only Finding / Likely cause / Confidence (full report stays in S3), failure notes are generic; the 'Suggested reply' section is gone — the operator writes to the customer | `feature/intercom-verified-org` (agent-factory), `feature/agent-factory-network` (tf-misc-infrastructure) | agent-factory, tf-misc-infrastructure |
| 2026-09-06 | Agent Factory worker sandbox hardening (security review 2026-09-03 AF-13, AF-14): worker runs with `tools: []` + `dontAsk` allow-list (three gateway tools), `settingSources: []`, non-essential traffic off, per-run `maxBudgetUsd`, and aborts if the session reports any other tool; MicroVMs egress through a customer-managed VPC connector (Terraform: egress SG allowing HTTPS only to new Bedrock-runtime + AgentCore-gateway interface endpoints and the S3 gateway endpoint; operator role) created by `scripts/create-network-connector.sh`, production refuses to launch without it; VM auth token scoped to port 8080; `PassNetworkConnector` pinned to the connector | `feature/intercom-verified-org` (agent-factory), `feature/agent-factory-network` (tf-misc-infrastructure) | agent-factory, tf-misc-infrastructure |
| 2026-09-05 | Auth background `?fx=glow` renderer — aiautomationsociety.ai-style drifting additive glow blobs (dark base, half-res canvas + CSS blur) inside the existing `?bg=` clip masks, coloured by `?palette=` plus Parchment secondary (purple) blobs in every palette; plasma stays the default renderer; mask edges drift gently (`?drift=0` pins); auth card centred on mobile with responsive padding + 4px white border | `feature/auth-graphics` | web-ui |
| 2026-09-05 | Cloudflare single-IP spike alerts: stop paging P1 on NoData / datasource error (both rules `no_data_state` + `exec_err_state` → OK). Repeated ">500 req/hr" Slack pages on 4-5 Sep were the NoData health-canary path — Cloudflare showed the top unmitigated IP at 49-74 req/hr in the alert window. Real threshold breaches still page | `feature/csm-agent-factory-link` | tf-misc-infrastructure |
| 2026-09-07 | erx-service Grafana dashboard: `partner_id` dropdown (custom var off the `PARTNER` enum, All = `.*`) wired through every panel — inline `details.partner_id` filter on the 22 panels whose events carry it, post-stats `coalesce(partner_raw, 'unknown')` filter on the 3 join panels (11/16/17) so All doesn't drop partner-less rows; Precheck panels (18/26) left unfiltered as that pm-metric emits no partner_id in prod. Also: `partner_id` column on "New Organizations Without Scripts" + "Successful Entity ID Provisions" (joined from partner-service `New Partner Integration`), new "New Prescribers per Week (first script)" barchart, and a fix for panels rendering "No data" — Grafana's CloudWatch datasource splits the stats clause on commas, so `strcontains` moved out of the aggregates into `fields` | `feature/misc-tweaks` | tf-misc-infrastructure |
| 2026-09-03 | Agent Factory run-id / correlation-id hardening (security review 2026-09-03 items AF-01, CM-01, AF-24, AF-25, AF-06): common validates caller-supplied `x-correlation-id`/ClientContext ids (`^[A-Za-z0-9._:-]{1,128}$`, else generated) and logs `correlation_id_source`; broker decides trace ownership from trusted emitter fields on unflattened events with tag-reuse detection (two inbound requests on one id → refused), binds runs to the invoking gateway (`gateway_id` vs `bedrockAgentCoreGatewayId`, `EXPECTED_GATEWAY_ID`), returns one public run error code, logs `trace_id`; tenant error predicate drops `actor_org`; admin chat mints a random run id per request; `dev-run.sh` random ids + UUID check + 2 h default | `main` (common), `feature/intercom-verified-org` (agent-factory), `feature/admin-auth-audit` (admin-backend) | common, agent-factory, admin-backend |
| 2026-09-03 | Intercom Investigate button: organisation scope now resolved from the contact's identity-verified `external_id` (Parchment user_id) via a new direct-invoke-only user-service lookup (`resolve-user-organization-stf`, returns org id + name), never from Messenger-writable contact/company attributes; organisation name shown in the canvas before launch and re-resolved on submit (security review 2026-09-03, AF-12); agent-factory now mounts `common` as a submodule and its Lambdas use the shared Powertools logger, `wrapHandler` and `invokeLambdaFunction` (correlation id propagates Intercom → launcher → user-service) | `feature/agent-factory-user-lookup` (user-service), `feature/intercom-verified-org` (agent-factory) | user-service, agent-factory |
| 2026-09-01 | deploy-all-with-sso.sh — one command deploys all serverless repos to dev/sandbox (per repo: branch switch, pull, common submodule sync, yarn, deploy-with-sso; dirty repos skipped, end-of-run summary) | `main` | common |
| 2026-09-01 | External API requestId now returns the correlation id (was an X-Ray root id that never appeared in log fields) — partner-quoted requestIds become directly traceable via logs / get_request_trace; X-Ray/random fallbacks kept outside request context | `main` | common |
| 2026-09-01 | Admin portal ↔ Agent Factory link: broker log tools (get_request_trace / get_recent_service_errors / get_recent_deployments) in the admin chat agent via SigV4 MCP to AgentCore Gateway; run records minted server-side from verified Cognito user + org picker (model never picks scope); task-role IAM (PutItem on runs table, InvokeGateway) + AGENT_GATEWAY_URL env | `feature/admin-auth-audit` (admin-backend), `feature/csm-agent-factory-link` (tf) | admin-backend, tf-misc-infrastructure |
| 2026-08-31 | Correlation-id propagation across direct Lambda invokes (invokeLambdaFunction injects ClientContext; deriveCorrelationId reads it — traces now survive service hops) + fix external-api-audit error_type always logging handler_error (read body.error.type per handleExternalError nesting) | `main` | common |
| 2026-09-02 | Intercom → Agent Factory "Investigate" button: Canvas Kit inbox app (Lambda Function URL, HMAC-verified, admin allowlist) resolves org scope from the linked contact/company attribute only, wraps ticket text as untrusted data, launches a MicroVM investigation and posts the report (with suggested-reply section) back as an internal note; per-conversation dedupe lock + global concurrency cap in the launcher | `feature/log-broker` | agent-factory |
| 2026-09-02 | Agent Factory tenant-scoped log broker (new `agent-factory` repo, CDK): MCP tools `get_request_trace`/`get_recent_service_errors`/`get_recent_versions` behind AgentCore Gateway + Cedar (LOG_ONLY), trusted org scope from AgentRuns table, ownership validation → NOT_FOUND_OR_NOT_AUTHORISED, redaction allow-list, query limits, audit events; deployed + smoke-tested end-to-end in dev via SigV4 MCP; Cedar flipped to ENFORCE 2026-09-02 (per-tool permits in policies/cedar/, default-deny + tools/list hiding verified live); execution layer 2026-09-02 — launcher Lambda + MicroVM agent worker (Claude Agent SDK on Bedrock au.opus-5, gateway tools only, non-root) with investigate-api-error skill, S3 report bucket; first acceptance run COMPLETED in 62s with a correct evidence-backed root cause | `feature/log-broker` | agent-factory |
| 2026-08-31 | Agent Factory requirements review + re-scope (decisions D-007–D-014: governed tool layer first, `au.*` Australia-resident Claude profiles, Claude Agent SDK chassis, thin launcher over Durable Functions, single tenant-scoped log broker, git+image-bake skills; Jira epic PAR-1096, story PAR-1097) | n/a (parchment-root docs/, not a repo) | none (docs only) |
| 2026-08-31 | eRx schema 30.1 prescriber types: new `R` = Designated Registered Nurse Prescriber (PBS/RPBS nurse prescribing from 1 Oct 2026) accepted end-to-end (PrescriberType unions/enums, precheck schema, prescriber-type dropdowns); `U` relabelled Nurse → Nurse Practitioner | `feature/nurse-prescriber-type-r` | common, erx-service, web-ui |
| 2026-08-29 | PBS prescriber number format validation (7 digits: 6 + check digit) + normalization at UI and API — shared common helper mirroring the AHPRA fix; pre-eRx check in create-erx-entity-id turns the generic E91037 (which the adapter self-heal misreads as an email collision) into a specific 400; web-ui create/update-user Zod schemas + prescriber-details form; user-service create/update schemas close the write paths that stored the 16-digit incident value; `Provider.prescriber_number` corrected `number`→`string` | `feature/prescriber-number-validation` | common, erx-service, user-service, web-ui |
| 2026-08-29 | External custom-drugs fixes (partner report): negative-value guard no longer rejects spaced strength ranges ("20 - 25 mg/g"); bulk create returns per-item `error.validation[]` on the all-failed 400 (optional validation param on ExternalApiHelpers.badRequest); GET org custom-drugs now honours the `custom_product_id` query filter | `feature/custom-drug-bulk-fixes` | user-service, common |
| 2026-08-27 | Partner ops scripts: confirm profile/stage/partner-id before running (typed 'production' gate for prod, `--yes` bypass for non-prod) | `feature/misc-tweaks` | partner-service |
| 2026-08-26 | AHPRA number format validation + canonical normalization at all provider write paths (shared common helper; closes unvalidated create/update-provider + schema-less prescriber form; pre-eRx check turns generic E91037 into a specific 400) + eRx adapter XML escaping of all Service Provider registration fields | `feature/ahpra-validation` | common, user-service, erx-service, erxadapter-service, web-ui |

| 2026-08-23 | Cross-org isolation enforcement (Jul-2026 review holes): add authz to `read-patientId-by-partner-patientId` + gate the found path in `search-create-partner-patient`; `bulk-upload-service` verifies the caller token against the path org via acl `check-stf` (fail-closed) and drops the insecure dev bypass; Fargate task-role `lambda:InvokeFunction` on acl `check-stf` + `STAGE`/`ACL_CHECK_STF_FUNCTION_NAME` env (IAM committed on `feature/partner-api-audit` — must deploy before the new bulk-upload image) | `feature/cross-org-enforcement` | patient-service, tf-misc-infrastructure |
| 2026-08-22 | Access-audit + Grafana watchdog: pipeline-failure alarms (Firehose delivery-stall/conversion-failure on 3 streams, responder Lambda errors/throttles) + Grafana self-monitoring (ECS-task & RDS dead-man's-switch, RDS low-storage) to `pm-<ws>-common-sns`; Cloudflare all-traffic spike rule alerts on NoData (silent datasource/schema-break guard); Grafana image default pinned to 12.3.1 | `feature/partner-api-audit` | tf-misc-infrastructure |
| 2026-09-19 | Prescription V3 (incl. UI polish: search shimmer, sticky patient banner, density toggle, 2-col wide layout, keyboard-flow tuning, shared FormFieldLabel/EmptyState; PBS listing footer "Select as Private" row + shared SelectPrivateRow; sport/pregnancy category definitions on hover; glass results header; URL prefill for partner links / embedded search SDK — `product_pack_id` + `pbs_code`/`private=1` (+`brand=1`) re-resolved against the drug index and applied through the same select path as a click, scalar overrides via the shared V2 contract, query string carried across the V2↔V3 rollout redirects; React Query for every V3 drug read — one cache for type-ahead / brand filter / PBS dialog / Select / prefill, removes duplicate MIMS fetches, fixes PBS-dialog + indication-search stale-response races; script + authority numbers as guarded queries (authority number no longer re-allocated on every panel remount); queue + template save as mutations; authority panel now shows for written/online authority and quantity/repeat overruns, not only phone-approval `restricted` items; patient-instruction quick-select chips hidden until panel hover or field focus, chips no longer steal focus on click; authority phone link + copy button removed from the tab order; V3 form → V2 `PrescribedItem` mapper `to-prescribed-item.ts` (TDD, 30 tests) wired into the submit footer — brand chip = suggested brand + brand's pack, new "Brand substitution not allowed" checkbox, streamlined items also get the 8-digit authority number, `SupplyDurationHint` reused, single dose / 250-char sig / eRx send-to-pharmacy tags to match V2 + erx-service; brand substitution now an icon-only padlock joined to the brand chip, with an amber state caption under the brand row; review-page hand-off — footer Send/Prescribe → NDE precheck (V2 popup reused) → existing V2 review page, S8/IHI/create/long-poll untouched; draft snapshot in sessionStorage written at hand-off and restored only on review-page Cancel (`?resume=1`, 8 h TTL, consume-once) through the prefill resolver, script + authority numbers reused; V3 page now runs the V2 ACL / onboarding / EULA gates; `reserved_1/2/3` seeded on V3; Enter in a field never sends; authority panel submit-time validation — approval number / streamlined code required when the panel is shown, error under the input, blocks Send/Queue/Template; patient strip — eScript readiness badge derived from the same IHI fields as patient-service `getEscriptStatus` (number + ACTIVE status + VERIFIED record) with the failing field named on hover, Paper/Electronic switch forced to Paper and disabled with the same reason when the IHI is not valid, shared `BackLink` (now takes a `className` for dark surfaces) left of the patient name; streamlined authority indication cell de-bolded); authority parity with V2 (V2 = oracle: every PBS restriction row scanned, `Restricted` no longer prompts, any "authority" type matches, missing PBS maxima never trigger, quantity seeded from pack size, DVA read from the patient so Unlisted RPBS works, no prescriber number forces + locks Private, Private checkbox drives the payload, generated authority number required at submit with Retry, approval number ≤10 chars, quantity 1–3000 whole, S8 / NSW S4B / TAS S4D repeat-interval requirement, urgent-supply confirm dialog, restriction badge = strongest row with four-tier colours; 200 V3 tests incl. hook-seam suite; responsive pass — drug-results PBS rows wrap below md instead of overflowing the panel (fixed-width table from md up, no sideways scroll), patient strip shows one datum per row until xl with the script number, eScript status badge and settings gear on the script row; patient-instruction quick chips always present but dimmed until the panel is hovered; submit footer rebuilt from the Claude Design canvas as one 36px row — action dropdown picks the route (patient / pharmacy / the org's Prescribe Don't Send copy), the middle carries only that route's field, draft and template become icon buttons with their own popovers, DeliveryMethodControl gains a chips variant and PopoverContent now extends the shared .br-glass-ios; review page Go back restores the draft again — the missing-payload guard is now a one-shot arrival check in a shared `useRequireReviewPayload` hook, so exit handlers that clear the store no longer race their own push; V3 review page `/prescriptions/new-v3` from the "Confirm eScript" design canvas — prescription-pad layout (sticky Rx header with script #, patient + prescriber cards, medication card with quantity / repeats / interval / est. supply-until and the administrative fine print, right rail "Check before sending" flags for S8 / PBS authority / state authority number / emergency / unusual / private / brand / Reg 24 / delivery, confirm card that names the recipient), same V2 create flow lifted into `useConfirmPrescription` (S8 Authsignal → IHI → create → long-poll, abort, analytics, V2 progress dialog reused), gated on the rollout flag with a fallback redirect to `/new`, V3 form now hands off here (embed still → V2 `/new`), Go back / create-another return to the V3 form, pure `review-model.ts` with 17 tests; SMS is the first channel chip in the footer row and draft popover; `search_medication`'s 100-char cap removed — after a selection the field holds the generated display string, so long combination-product names blocked submit; send-to-patient channel now defaults to the patient's mobile when one is recorded and SMS is enabled, email otherwise, and the Submit button carries the selected channel / route icon) | `feature/prescription-v3` | web-ui |
| 2026-08-12 | External create-patient: stop partner_id validation error from enumerating every registered partner slug (custom Zod errorMap, generic message) | `main` | common, patient-service |
| 2026-08-17 | NASH cert renewal + cutover (executed 16 Aug: 345/346 entities): eRx §3.6.4 per-entity push scripts, new-chain truststore support, cutover runbook + 2028 procedure | `feature/misc-tweaks` | healthcareid-service, erxadapter-service |
| 2026-09-15 | Integration tiles for Thrivio Health, Thrive Holistic Centre, MyLeaf, Dr James, BioStack Health, Hair Doctors Telehealth, Core Practice, Solo (PARTNER enum + logo/form components + accordion tiles, hidden in production; Hair Doctors Telehealth and Solo fully hidden — Solo reuses the demo logo until artwork arrives) | `integration-tiles` | web-ui, common |
| 2026-08-10 | In-app feature announcements (markdown posts published to S3/CloudFront via script — no redeploy; corner-card popup + bell feed in web-ui) | `feature/announcements` | web-ui, tf-misc-infrastructure |
| 2026-08-09 | WA conformance (PAR-1072): print min repeat interval next to repeats on paper prescription PDF; precheck skip-reason metrics with state/schedule context; Grafana precheck-outcomes panel | `feature/wa-conformance` | web-ui, erx-service, tf-misc-infrastructure |
| 2026-08-06 | eScript Pending badge click-through: popover explaining why a prescriber is pending (grouped by who can act: user EULA acceptance, admin profile/HPI-I fixes, automatic MIMS provisioning); users list now returns derived `onboarding` so the profile gate is evaluable | `feature/escript-pending-details` | web-ui, user-service |
| 2026-08-05 | Stay on prescription page after queuing a script (confirmation dialog + form reset instead of redirect to patient profile) | `feature/queue-script-stay-on-page` | web-ui |
| 2026-07-31 | Host-only SSO cookies + legacy .parchment.health migration (fixes cookie-bloat 403/431) | `bug/sso-cookie-bloat` | web-ui |
| 2026-07-30 | User analytics module (event pipeline, funnels, struggle/stuck signals, heatmaps) | `feature/user-analytics` | analytics-service, web-ui, tf-misc-infrastructure |
| 2026-07-20 | MIMS CDS drug interactions | `feature/mims-drug-interactions` | partner-service, web-ui |
| 2026-07-18 | Activation nudge system (D+2/7/14 state-aware nudges to org owner) | `feature/activation-nudges` | user-service |
| 2026-07-14 | Test framework + CI (Vitest / Storybook / Playwright, tiered GH Actions gating) | `feature/test-framework-ci` | web-ui |
| 2026-09-20 | MIMS flat file (incl. `/mims-local/search` results ordered by ascending molecule strength, mg-normalised, no-strength packs last, relevance order kept within ties) | `feature/mims-flat-file` | partner-service, tf-misc-infrastructure |


## Released
| Date | Feature | Branch | Repos affected |
|---|---|---|---|
| 2026-09-19 | HI Service search by email + mobile | `feature/HI-email-phone` | healthcareid-service |
| 2026-09-19 | Settings: show the Billing tab for consolidated partner child orgs (integrated_pms set, no own Stripe customer) so the partner-billed view with provider tier selection is reachable; drops the extra org read in the settings shell | `feature/billing-tab-consolidated-children` | web-ui |
| 2026-09-14 | Remove Sentry from web-ui (drop `@sentry/nextjs`, the `withSentryConfig` build wrapper, client/server/edge init files, example page + API route, wizard `global-error`/`_error` pages; MIMS auth failures and SSO timings now go to console/Amplify SSR logs, client errors stay on CloudWatch RUM) | `feature/remove-sentry` | web-ui |
| 2026-09-19 | Past-due banner: shown to every user of a past_due org (not only billing admins) with an explicit note that service may be disrupted until the payment method is updated; admin CTA unchanged | `feature/past-due-banner-disruption` | web-ui |
| 2026-09-19 | Per-item update/delete for organisation-level custom drugs (external PUT/DELETE `/v1/organizations/{organizationId}/custom-drugs/{customDrugId}`, internal owner/admin-gated PUT/DELETE for the web UI, bulk-create response returns `created[]` ids, org catalog GET honours `?custom_product_id=`, integration tests; TF endpoint module + audit log groups; web-ui: Product ID column in the drug table and `custom_product_id` rule aligned with the API; partner docs incl. catalog sync workflow page; org-tab edit/delete in web-ui still to come) | `feature/org-custom-drug-item-ops` | user-service, tf-misc-infrastructure, web-ui |
| 2026-09-19 | External IHI precheck endpoint for partners (live HI dry-run of create; runs the full identifier cascade incl. mobile/email, reports `searched_path` + `failed_paths`) | `feature/patient-ihi-precheck` | patient-service, tf-misc-infrastructure | yes
| 2026-09-19 | IHI validation lookup (search outcome shown in a result dialog — verified / guided-fix no-match checklist / conflicts — replacing the inline verified panel; shared StatusBadge, amber for any non-Active/Verified IHI status; address now its own required section and saved on the created patient — it was silently dropped because the HI Service never returns one; wire mapping extracted to `_lib/lookup-payloads.ts`; shared PmDatePicker fix: pasting into an untouched date field did nothing); IHI search identifier fallback cascade in searchHS (404 no-record falls back to next identifier; new priority IHI > Medicare > DVA > mobile > email > addresses; matched/failed identifiers + warning notify in response; 120s timeouts; phone accepts AU mobiles only in create/update schemas + partner docs; lookup form sends ALL supplied identifiers so the backend cascades, verify-method picker removed — IHI/Medicare/DVA are always-visible optional fields, phone/email optional contact fields under Address, fallback warning shown on verified banner, submit renamed Verify patient; patient edit page reskinned with the same design system — SectionRow card + IHI-status/Search-order/danger-zone sidebar, single shared patient fetch replacing 7 per-section fetches, revalidate cascade surfaced) | `feature/ihi-validation-refactor` | web-ui, patient-service, common | yes
| 2026-09-19 | Nightly MIMS subscription cleanup cron (dev/sandbox only, prod-gated 4 ways) | feature/mims-cleanup-cron | user-service | Yes|


| Date | Feature | Branch | Repos affected |
|---|---|---|---|
| 2026-09-14 | Stripe Subscriptions Grafana dashboard: trial vs paying vs past-due vs churned vs never-subscribed customers — daily subscription-status snapshot pm-metric Lambda (aggregate + per-partner + per-org lines incl. billing contact name/email; Lambda recovered from an uncommitted stash and committed 2026-09-14; partner_id now falls back to the org row by id so orgs migrated to consolidated billing report `integrated_pms` instead of `unknown`; merged straight to main and released to production 2026-09-14) + dashboard with mix/trend/partner/org tables and webhook lifecycle events | `feature/stripe-subscription-metrics` | stripe-service, tf-misc-infrastructure |
| 2026-08-29 | User analytics module (event pipeline, funnels, struggle/stuck signals, heatmaps) | `feature/user-analytics` | analytics-service, web-ui, tf-misc-infrastructure | 

| Date | Feature | Branch | Repos affected |
|---|---|---|---|
| 2026-08-23 | Partner API activity audit: one structured `external-api-audit` line per partner-facing call (wrapHandler `isExternal` middleware across 27 external handlers + per-branch `/v1/token` outcomes) and per outbound webhook delivery; CloudWatch→Firehose→Parquet→Athena pipeline (`partner_api_audit` + API Gateway v1 access-log tap `partner_gateway_access`) on the existing audit bucket/CMK/workgroup; Partner API Audit Grafana dashboard; `query_partner_api_activity` admin-chat tool (allowlist-validated Athena) + csm-agent IAM | `feature/partner-api-audit` | common, partner-service, patient-service, erx-service, user-service, admin-backend, tf-misc-infrastructure | 

| Date | Feature | Branch | Repos affected |
|---|---|---|---|
| 2026-08-17 | Prescriber account: business address now fully required (all 5 fields, was state only) with red required markers, per-field errors and panel error outline; grey literal "*" on Prescriber details / ERX details panels replaced with a shared red RequiredMark | `feature/prescriber-required-fields` | web-ui |
| 2026-08-17 | PAR-1073: HPI-O/CSP link error now offers a "Click here to view our guide" link that opens the help article inside the Intercom Messenger (falls back to a new tab) — shown on onboarding step 1 and org settings; useOrganization now throws on a failed org fetch instead of caching the error body as an Organization (a fetch failure previously showed a false "validate your HPIO" banner blocking team invites) | `feature/csp-error-help-guide` | web-ui |
| 2026-08-17 | Onboarding wizard: Next Step button now blocked on step 1 until org setup (HPIO) is actually saved — closes a bypass that let users reach PMS integration/team invite with an unsaved HPIO | `feature/onboarding-next-step-gate` | web-ui |


| Date | Feature | Branch | Repos affected |
|---|---|---|---|
| 2026-08-13 | Billing-gap fixes: seat sync provisions Stripe customer on 'UNKNOWN' sentinel, GST tax-location self-heal for pre-Jul-25 customers, tiered prices no longer cached as $0 | `feature/billing-gap-fixes` | stripe-service, partner-service |

| Date | Feature | Branch | Repos affected |
|---|---|---|---|
| 2026-08-08 | eScript Pending badge click-through: popover explaining why a prescriber is pending (grouped by who can act: user EULA acceptance, admin profile/HPI-I fixes, automatic MIMS provisioning); users list now returns derived `onboarding` so the profile gate is evaluable | `feature/escript-pending-details` | web-ui, user-service | 
| 2026-08-08  | Lowercase email at all sign-up entry points (case-sensitive Cognito pool created duplicate users) | `feature/email-case-insensitive-signup` | user-service, auth-service, web-ui 
| 2026-08-08 | HPI-I conflict on provider create: surface + log, and supersede stale records of disabled/deleted holders (fixes silent onboarding loop for duplicate accounts) | `feature/hpii-conflict-feedback` | user-service, web-ui | 
| 2026-08-08  | Endpoint hardening — email, auth, user, kms-cognito, web-ui | `bug/misc-bugs` | kms-cognito, email-service, auth-service, user-service, web-ui | 
| 2026-08-08 | Register partner org ops script | `feature/register-partner-org-ops-script` | partner-service | 
| 2026-08-08  | Fix deep-link redirect after login timeout (preserve authRedirect cookie) | `fix/login-redirect-continuity` | web-ui | 
| 2026-08-08  | Consolidated tier selection + FLAT multi-org quantity merge (fixes duplicate-price sync failure); setup script takes mandatory --product-id; fix canceled-sub paywall bypass (subscriptions.list status:all) | `feature/consolidated-tier-selection` | stripe-service, web-ui | 


| Date | Feature | Branch | Repos affected |
|---|---|---|---|
| 2026-07-27 | Raise prescription quantity cap from 199 to 999 | `bug/quantity-max-999` | web-ui |
| 2026-07-26 | Access-audit pipeline (CWL → Firehose Parquet archive + Athena + deny responder) — deployed to production | `main` | tf-misc-infrastructure |


